import { createSlice } from "@reduxjs/toolkit";

const initialState = ({currentUser: null});
/*
const useSlice = createSlice({name:user, 
                        initialState, 
                        setCurrentUser = (state, action) => {

                        }
                    });
                    */
